Fifth project
the continuation of data structures 

~task explanantion~

task 0:
.append(x) is a method
adds one thing to the end of the list
    e.g|                        result|
        my_list = []                    my_list == [5]
        my_list.append(5)
        
task 1:
----

task 2:
set() is a function
creates a set from a list(builds a box)
.add(x) is a method
adds item to a set(put things into the box)
    e.g|                        result|
        numbers = set()                 numbers = {1,20}
        numbers.add(1)
        numbers.addd(20)

task 3:
----

task 4:
----

task 5:
----

task 6:
sorted()
 is a function
 sorts the items of a list in order based on smallest to biggest
    e.g|                                result|
        nums = [3, 5, 1, 7, 8]                  new_nums = [1, 3, 5, 7, 8]
        new_nums =sorted(nums)
