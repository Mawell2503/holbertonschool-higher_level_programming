#!/usr/bin/python3
def number_keys(a_dictionary):
    #  variable for the count of keys
    count = 0
    #  loop through dictionary
    for key in a_dictionary:
        #  count adding up
        count = count + 1
    return count
